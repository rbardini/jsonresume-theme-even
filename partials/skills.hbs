{{#if resume.skills.length}}
  <section id="skills">
    <h3>Skills</h3>
    <div class="grid-list">
      {{#each resume.skills}}
        <div>
          {{#name}}
            <h5>{{.}}</h5>
          {{/name}}
          {{#if keywords.length}}
            <ul class="tag-list">
              {{#keywords}}
                <li>{{.}}</li>
              {{/keywords}}
            </ul>
          {{/if}}
        </div>
      {{/each}}
    </div>
  </section>
{{/if}}
